## 第 16.3 节：Amelia 逐个故事实现

Amelia 是开发者智能体。她不是通用助手——她是一个专注的实现智能体，有一个严格的操作约束：精确实现故事所说的内容，不多也不少。如果她发现模糊性或冲突，她会停下来并标记出来。她不会默默地解决它。
调用 Amelia：`/bmad-agent-bmm-dev`。Claude Code 采用 Amelia 角色，Amelia 读取她被分配的故事文件。
### Amelia 的工作节奏

每个 Amelia 会话遵循相同结构：
1. 读取故事文件（仅分配的故事，不读其他）
2. 读取上下文节中列出的文件
3. 按顺序实现验收标准
4. 编写测试要求节中列出的测试
5. 运行测试
6. 将故事状态更新为"done"

当所有验收标准通过且所有测试通过时，会话结束。如果有测试失败，Amelia 修复实现——她不会测试失败时标记故事完成。
### Amelia 实现 Story-001

调用：
```
/bmad-agent-bmm-dev

"实现 story-001。故事文件位于
_bmad/stories/story-001.md。在前端 web/ 中工作，
在后端 api/ 中工作。"
"

Amelia 读取 story-001.md。她从上下文节中注意到 Vite 项目存在，有一个空白的 App.tsx，FastAPI 项目存在，只有一个健康检查。她从前端开始。

**第 1 步：创建 RegistrationForm 组件**

```typescript
// web/src/components/RegistrationForm.tsx
import { useState } from 'react'
import { supabase } from '../lib/supabase'
export function RegistrationForm() {
 const [email, setEmail] = useState('')
 const [error, setError] = useState('')
 const [showSuccess, setShowSuccess] = useState(false)
const isValidEmail = (email: string) =>
    /^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(email)

async function handleSubmit(e: React.FormEvent) {
    e.preventDefault()
    if (!isValidEmail(email)) {
      setError('请输入有效的电子邮件地址')
      return
    }
    setError('')
    const { error } = await supabase.auth.signInWithOtp({ email })
    if (error) {
      setError(error.message)
    } else {
      setShowSuccess(true)
    }
}
if (showSuccess) {
    return (
      <div className="text-center p-8">
        <p className="text-gray-600">
          检查您的电子邮件以获取登录链接
        </p>
      </div>
    )
}
return (
    <form onSubmit={handleSubmit} className="space-y-4">
      <div>
        <input
          type="email"
          value={email}
          onChange={e => setEmail(e.target.value)}
          placeholder="you@example.com"
          className="w-full px-3 py-2 border rounded-md"
        />
        {error && <p className="text-red-600 text-sm mt-1">{error}</p>}
      </div>
      <button
        type="submit"
        className="w-full bg-blue-600 text-white py-2 rounded-md"
      >
        发送魔法链接
      </button>
    </form>
)
}
```

**第 2 步：更新 App.tsx**

```typescript
// web/src/App.tsx
import { RegistrationForm } from './components/RegistrationForm'
export default function App() {
 return (
    <div className="min-h-screen flex items-center justify-center">
      <div className="w-full max-w-md p-8 bg-white rounded-lg shadow">
        <h1 className="text-2xl font-semibold mb-6 text-center">
          登录 OpenTalon
        </h1>
        <RegistrationForm />
      </div>
    </div>
)
}
```

**第 3 步：添加 API 端点**

```python
# api/src/opentaion_api/routers/auth.py
from fastapi import APIRouter, HTTPException
from pydantic import BaseModel, EmailStr
from ..supabase import supabase_admin
router = APIRouter(prefix="/auth", tags=["auth"])
class RegisterRequest(BaseModel):
    email: EmailStr

@router.post("/register")
async def register(request: RegisterRequest):
    """为给定的电子邮件地址触发魔法链接邮件。"""
    try:
        supabase_admin.auth.admin.generate_link(
            type="magiclink",
            email=request.email
        )
        return {"message": "检查您的电子邮件以获取登录链接"}
    except Exception as e:
        raise HTTPException(status_code=500, detail=str(e))
```

**第 4 步：编写测试**

```python
# api/tests/test_auth.py
import pytest
from httpx import AsyncClient
async def test_register_valid_email_returns_200(client: AsyncClient):
    response = await client.post("/auth/register",
                                  json={"email": "test@example.com"})
    assert response.status_code == 200

async def test_register_invalid_email_returns_422(client: AsyncClient):
    response = await client.post("/auth/register",
                                  json={"email": "not-an-email"})
    assert response.status_code == 422
```

Amelia 运行测试，确认它们通过，并更新故事：

```markdown
## 状态：done
```

### 严格约束在实践中的应用

在 story-001 的实现过程中，Amelia 注意到在注册的同时添加会话管理会使认证流程感觉更完整。她没有添加。范围外部分说"会话持久化（story-003）。"她在故事文件底部的一条简短注记中标记了这个观察："注：会话处理是 story-003。当前实现不持久化会话。"

这种纪律正是使逐个故事方法可靠的原因。Amelia 不做范围决策。她实现被指定的内容。

### 在 OpenTalon 中

在 OpenTalon 中，Amelia 在多个 Claude Code 会话中按顺序完成所有 24 个故事。每个会话以 `/bmad-agent-bmm-dev` 调用和对当前故事的引用开始。每个会话在故事被标记为 done 时结束。人类在进入下一个故事之前审查每个故事的输出——不是详细检查代码，而是验证验收标准是否满足以及没有发生明显的架构偏离。这种审查每个故事五分钟，而不是三十分钟。故事文件的验收标准使审查变得快速。Quinn 的测试验证行为。
